<%- include('./partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <ul>
            <% categories.forEach(cat=>{ %>
                <a href="/category/<%= cat.category_id %> ">
                    <li>
                        <%= cat.name %>
                    </li>
                </a>
                <%})%>
        </ul>

    </main>

    <%- include('./partials/footer') %>